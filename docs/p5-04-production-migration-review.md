# P5-04: 本番移行判断レビュー（P5 パイロット結果）

設計書 §9.1 P5-04 に基づき、P5-01〜P5-03 の成果を踏まえて次マイルストーン判断を記録する。

## 結論

- **P5 の目的（IPC 経路の検証）は達成**。
- ただし **本番移行（既存 GUI の全面置換）判断は保留**。
- 次マイルストーンでは、Tauri へ全面移行する前に UI 再設計範囲を確定する。

## 根拠

1. `src-tauri` を含む workspace 構成で両クレートを継続ビルド可能（P5-01）。
2. 最小フロントから invoke 相当で P4 DTO 読み取りが成功（P5-02）。
3. Windows 手順が文書化され、再現可能性が担保された（P5-03）。

## 現時点での不足（本番移行前に必要）

- Tauri 本番 UI の画面遷移/情報設計（既存 egui と同等以上の UX 要件）
- IPC エラー表示・リトライ・ジョブ進捗表示の UX 詳細
- 配布/更新運用（インストーラ、更新方針）

## 次マイルストーン合意案

- **M1**: Tauri 側で「分析実行開始〜完了表示」までの縦断フローを 1 本実装
- **M2**: 条件編集・DB Viewer の責務分割方針を固める
- **M3**: 本番移行可否（全面移行/併存運用）を再判定

## 改訂

| 日付 | 内容 |
|------|------|
| 2026-03-23 | P5-04 初版 |
